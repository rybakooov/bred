export default function ({ redirect, route, $auth }) {
  // console.log(app.redirect)
  // if (app.$auth) {
  //   if (app.$auth.user.is_staff) {
  //   }
  // }
  // console.log(123)
  // console.log(redirect)
  // console.log(route)
  // console.log($auth)
}
